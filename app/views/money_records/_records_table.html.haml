%tr.table-row
  %td= record.created_at.to_date
  - if record.amount < 0
    %td.red-record= record.amount
  - elsif record.amount >= 0
    %td.blue-record= record.amount
  %td
    %button.left.edit Edit
    %button.right.delete Delete
